# Milestones

Each milestone is worked on its own `feature/<name>` branch (`lvx feature
start`/`lvx feature finish`, see `version_policy.md`). A milestone's definition of
done requires all of: docs updated, requirements created/updated in the
requirements DB (`REQ-#########`, see `requirements_policy.md`), code implemented,
automated tests written, and those tests passing. Requirement IDs are listed as
"TBD" below because none are created until their milestone actually starts, per
the documentation policy.

All UI work from M9 onward — new screens and restyles alike — follows the
design system in [`ui_design.md`](ui_design.md) (palette, typography, the
cookbook/fridge/pinned-paper metaphors, and the shared mobile tab bar /
desktop rail shell). A milestone whose objective mentions "per
`ui_design.md`" is not done until its screens match that system, in addition
to the usual functional definition of done.

| ID | Objective | Requirements | Status |
|---|---|---|---|
| M0 | Project bootstrap: folder layout, `docs/`, `.lvx/config.json`, `VERSION`. No code. | — (no implementation, not requirement-tracked) | Done |
| M1 | Recipe ingestion pipeline: Ollama-backed extraction service (cook time, classification, nutrition/calorie estimate, ingredients, steps from free-text input), recipe storage with a liked/disliked preference state, backend API for recipe CRUD, minimal recipe-library UI. First introduction of `src/`, `tests/`, and the project's Bazel build files. The liked/disliked preference state was removed entirely in M13. | REQ-000000001, REQ-000000003, REQ-000000004, REQ-000000005, REQ-000000006, REQ-000000007, REQ-000000008, REQ-000000009 — REQ-000000002 (preference persistence) deleted by M13 | Done |
| M2 | Household configuration (shared by every device, not per person): recipes-per-week count, day/time for weekly recommendations, food preferences, number of AI suggestions per plan, default servings per meal — CRUD API + settings UI. | REQ-000000010, REQ-000000011, REQ-000000012 | Done |
| M9 | Visual design system adoption: build the shared base template/CSS per `ui_design.md` (tokens, Fraunces + Inter, the mobile bottom-tab / desktop left-rail shell) and restyle M1's recipe library, recipe detail (including the fridge-door ingredient toggle), and add-recipe screens, plus M2's settings screen, to match. No new functionality — a visual retrofit of what M1/M2 already shipped. | REQ-000000013 to REQ-000000017 | Done |
| M3 | Weekly meal plan generation from stored recipes: scheduler trigger, selection engine that fills a plan from the existing library (excluding disliked recipes) up to the configured recipe count, plan review UI presented as the open-cookbook layout from `ui_design.md` — meals aren't assigned to days, the household picks and cooks them in any order, and can mark a meal cooked directly (the pot-stamp toggle) without going through cook-along. The "excluding disliked recipes" clause no longer applies since M13 removed the preference concept - selection now draws from the whole library. | REQ-000000018 to REQ-000000021 (REQ-000000018 edited by M13) | Done |
| M4 | ~~AI-suggested recipes and reroll~~ — **removed entirely, see M10.** Originally: combination-of-stored-recipes generation, extraction via the Milestone 1 pipeline, like/dislike UI at suggestion-review time, plus reroll actions — whole-plan reroll, single-meal reroll, and controlled reroll (up to 10 unused liked library recipes for one meal, presented as the single-select picker from `ui_design.md`). Online search-based generation (`SearchProvider` protocol, tried before combination) was implemented against **Spoonacular** — chosen over Tavily, Exa, You.com, SerpAPI, Serper.dev, Edamam, TheMealDB, and self-hosted SearXNG for returning structured recipe data directly (no page-scraping) and filter parameters that mapped onto `food_filter`, at a free tier (~150 req/day) far beyond this household's actual usage. The reroll actions survive M10's removal, redefined as library-only (no more AI-generation fallback). | REQ-000000025, REQ-000000026 (redefined), REQ-000000027, REQ-000000028 (redefined) — REQ-000000022, REQ-000000023, REQ-000000024, REQ-000000038, REQ-000000041, REQ-000000042 deleted by M10 | Removed |
| M5 | Shopping list generation: ingredient merge/dedup across a finalized plan, quantity scaling to each recipe's servings (including per-recipe override away from the default), checkbox UI styled as the pinned paper note from `ui_design.md`, actual-cost entry. | REQ-000000029 to REQ-000000032 | Done |
| M6 | Guided cook-along mode: step-by-step walkthrough UI for a recipe's cooking steps as the large single-step cards from `ui_design.md`, ending with a cooked/left-uncooked prompt that sets the same cooked flag M3's pot-stamp toggle controls. (This milestone's original objective described the end-of-cook-along prompt as also updating a like/dislike preference state; the prompt never actually did that in the shipped code, and M13 has since removed the preference concept entirely, so this row is corrected to match what was actually built.) | REQ-000000033 to REQ-000000034 | Done |
| M7 | Weekly scheduling end-to-end: the M3 scheduler actually firing at the user-configured day/time and notifying the user a new plan is ready. Notification is an in-app banner (no email/push channel exists) and the trigger polls every 60s rather than firing one precisely-timed job - see `architecture.md`'s Scheduler entry, including its known UTC-interpretation limitation for `recommendation_time`. | REQ-000000035 to REQ-000000037 | Done |
| M8 | Remote access: Tailscale set up on the home server, tailnet ACLs restricting access to the two household members, `tailscale serve` (not `funnel`) exposing the app over HTTPS via MagicDNS, both phones enrolled in the tailnet. See `architecture.md`'s "Remote access & network security" section. | — (host/network setup, not requirement-tracked, per M0's precedent) | Done |
| M10 | Complete removal of Spoonacular and the AI-suggestion/recipe-recommendation system introduced in M4: `planning/suggestion.py`, `planning/spoonacular_fields.py`, `planning/spoonacular_import.py`, `vault.py`, the `lmeals import-spoonacular` CLI command, the `/settings/recipe-preferences` page, the shopping-list ingredient-substitutes lookup, and the household's `food_preferences_text`/`food_filter`/`ai_suggestions_per_plan` fields are all deleted outright. Meal planning now draws exclusively from recipes already saved in the cookbook (M3's selection engine); reroll (whole-plan, single-meal, controlled) survives, redefined to only ever swap in unused liked library recipes rather than falling back to generating something new (M13 later dropped the "liked" qualifier too - see its entry). Household preferences and meal-plan DB schemas gained migrations dropping the now-dead columns/tables. Decided as a deliberate product direction: Spoonacular didn't fit this household tool well enough to keep, and the automatic-recipe-collection idea is abandoned rather than replaced. | REQ-000000044; REQ-000000022, REQ-000000023, REQ-000000024, REQ-000000038, REQ-000000041, REQ-000000042, REQ-000000043 deleted; REQ-000000010, REQ-000000012, REQ-000000025 to REQ-000000028 edited (REQ-000000028 later deleted, REQ-000000025 to REQ-000000027 further edited, by M13) | Done |
| M13 | Removal of the liked/disliked recipe-preference feature: the `Preference` enum/field, the recipe-library heart toggle, the plan-review thumb-up/thumb-down pair, the recipe-detail preference badge, the `PATCH /api/recipes/{id}/preference` and `POST /recipes/{id}/preference`/`POST /plan/meals/{id}/preference` endpoints, and the JSON API's `?preference=` list filter are all deleted outright. The selection engine and both reroll paths (single-meal, controlled) no longer filter by preference - they now draw from every recipe in the library rather than only ones marked "liked". Cook-along's end-of-session cooked/left-uncooked prompt is unaffected (it never actually touched preference in the shipped code - M6's milestone description and other docs describing a cook-along "like/dislike prompt" were already stale before this milestone; see the corrections on M6/M10's rows and in `architecture.md`/`design.md`/`ui_design.md`). Decided as a deliberate product simplification: the household found liking/disliking recipes added friction without changing how they actually used the cookbook - a recipe they don't want to cook again, they now just delete or leave alone rather than tracking a preference state for it. | REQ-000000006, REQ-000000018, REQ-000000025 to REQ-000000027 edited; REQ-000000002, REQ-000000028 deleted | Done |
| M11 | Remote deployment mechanism: `lmeals serve` runs as a `systemd --user` service on the home server (auto-restart on crash, survives logout via `loginctl enable-linger`, starts on boot) instead of a foreground terminal process, and a `bazel run //:deploy` target rebuilds the wheel (`//:install`) and restarts that service in one step. Supports the actual dev workflow: development happens by sending prompts to a Claude Code session on the home server via Claude Remote Control, `//:deploy` is run manually once a change is ready, and the updated app is then reachable over the M8 tailnet with no separate step — `tailscale serve` keeps pointing at the same local port across restarts. See `architecture.md`'s "Deployment" section. | — (host/network setup plus an untested tooling script, not requirement-tracked, per M0's/M8's precedent) | Done |
| M12 | Recipe add/edit rework: a single "recipe edit page" (shared between "New recipe" and "Edit recipe") with directly-editable fields — name, nutrition (calories/protein/fiber, all optional), an ingredients table (independent rows, name + quantity columns, add/remove row), and cooking steps (one row per step, add/remove row) — plus save/cancel. "New recipe" drops M1's Ollama free-text-extraction flow (index-card textarea → "Simmering…" → preview card) in favor of opening this page pre-filled empty; "Edit recipe" opens the same page pre-filled from an existing recipe. A "Duplicate recipe" action on the recipe detail page copies an existing recipe (name suffixed " (copy)") and drops the household straight into its edit page. Per `ui_design.md`'s retrofit-scope precedent, this supersedes the M1/M9 "Add recipe" screen description rather than extending it. | REQ-000000045 | Implemented, pending manual UI validation |
| M14 | Weekly plan lifecycle: (1) the pot-stamp "Mark cooked" toggle now only shows once a plan is finalized (a draft shows none), and once every meal in a finalized plan is cooked, "Cancel Plan" is replaced by "Plan Ahead" (posts to the same `/plan/generate` action, superseding the finished plan with a fresh draft); (2) the household can add/remove individual meal slots directly on a draft plan page ("odd week" adjustment) without touching the `recipes_per_week` Settings default; (3) the existing recommendation-day/time automation gains an on/off toggle (`recommendation_enabled`, default on) and now force-finalizes whatever plan is current before generating the next draft, so a forgotten manual confirm doesn't leave an orphaned draft behind; (4) a new, independent "Auto-confirm plan" schedule (`auto_confirm_enabled`/`auto_confirm_day`/`auto_confirm_time`, default off) auto-finalizes whatever plan is currently a draft, letting the household never need to click "Confirm Plan" themselves. | REQ-000000046 to REQ-000000049 | Implemented, pending manual UI validation |

## Sequencing notes

- M1 must land before M3–M6, since every later milestone operates on recipes the
  M1 pipeline produces.
- M9 (visual design system) should land before M3, so M3–M6 build their screens
  once against `ui_design.md` directly rather than shipping placeholder styling
  and needing a second restyle pass later. It only depends on M1/M2 (the
  screens it restyles), so it can start as soon as they're done.
- M2 (preferences) must land before M3 (meal plan generation) and M4 (AI
  suggestions), since both consume preference configuration.
- M7 depends on M2 (day/time configuration) and M3 (something to generate on
  schedule).
- M5 (shopping list) depends on a finalized plan, which depends on M3 and, if AI
  suggestions are enabled, M4.
- M8 only needs M1 (something running to expose) and can otherwise be done at any
  point in parallel with M2–M7 — it's host/network configuration, not application
  logic, so it doesn't block or get blocked by the feature milestones.
- M11 depends on M8: `tailscale serve` needs to already be pointed at the local
  port before a systemd-managed, always-restarting `lmeals serve` is useful —
  otherwise there's nothing on the tailnet for a deploy to update. Like M8, it's
  host setup plus a small Bazel target, not feature work, so it doesn't block or
  get blocked by M9/M10.
- M12 depends on M1 (the recipe storage/API it reworks the editing UI for) and
  M9 (it must match `ui_design.md`'s established system, even though it also
  supersedes that doc's original "Add recipe" screen description). It doesn't
  depend on M2–M8, M10, or M11.
- M13 depends on M1 (the preference field it removes) and M3/M4/M10 (the
  selection/reroll logic it changes to stop filtering by preference). It's
  independent of M12 - both touch recipe-related UI but not the same code.
- M14 depends on M2 (household preferences, which it extends), M3 (the plan
  store/selection engine it builds add/remove-meal and force-finalize on top
  of), and M7 (the scheduler it adds the second auto-confirm check to). It's
  independent of M12/M13.
